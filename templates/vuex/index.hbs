const state = {
{{  name }}: []
};

const mutations = {
set{{ properCase name }}: (state, {{ name }}) = > {
if({{ name }}) {
state.{{ name }} = {{ name }};
} else {
state.{{  name }} = [];
}
}
};

const actions = {
set{{ properCase name }}({
commit
}, {{ name }}) {
commit('set{{ properCase name }}', {{ name }});
}
};

export default {
namespaced: true,
state,
mutations,
actions
};
